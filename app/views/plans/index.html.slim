.db-v2-header
  .db-v2-header__left
    h1.db-v2-header__title = t("recrivo.auth.plans_title")
    p.db-v2-header__subtitle = t("recrivo.auth.plans_subtitle")
  .db-v2-header__right
    = render "shared/lang_switch"
    = render "/dashboard/theme_switch"
    - unless @is_new_account
      = link_to dashboard_subscription_path, class: "db-v2-btn db-v2-btn--ghost" do
        i.fa-solid.fa-arrow-left aria-hidden="true"
        span = t("recrivo.auth.plans_back")

- trial_config = Subscription.plan_config(:trial)
- if @is_new_account
  .db-v2-alert.db-v2-alert--info
    i.fa-solid.fa-circle-info aria-hidden="true"
    div
      p
        = t("recrivo.auth.plans_trial_prefix")
        strong = t("recrivo.auth.plans_trial_strong", days: Subscription::TRIAL_DAYS)
        = t("recrivo.auth.plans_trial_suffix", scenarios: Subscription.format_limit(trial_config[:situation_limit]), interviews: Subscription.format_limit(trial_config[:monthly_interview_limit]))

= render "plans/pricing_slider"
