<div class="meetia-auth__page">
  <%= link_to locale_root_href, class: "meetia-auth__brand" do %>
    <%= image_tag "recrivo_logo.png", alt: "AI Interview", class: "meetia-auth__logo" %>
  <% end %>

  <div class="meetia-auth__lang">
    <%= render "shared/lang_switch" %>
  </div>

  <div class="meetia-auth__card">
    <h1 class="meetia-auth__title"><%= t("recrivo.auth.password_forgot_title") %></h1>
    <p class="meetia-auth__lead"><%= t("recrivo.auth.password_forgot_lead") %></p>

    <%= form_for(resource, as: resource_name, url: client_password_url_for_locale, html: { method: :post }) do |f| %>
      <%= render "clients/shared/error_messages", resource: resource %>

      <div class="meetia-auth__field">
        <%= f.label :email, t("recrivo.auth.email"), class: "meetia-auth__label" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "meetia-auth__input" %>
      </div>

      <%= f.submit t("recrivo.auth.submit_password_email"), class: "meetia-btn meetia-btn--primary meetia-btn--lg meetia-auth__submit" %>
    <% end %>
  </div>

  <div class="meetia-auth__links">
    <%= render "clients/shared/links" %>
  </div>
</div>
