<p><%= t("recrivo.mailer.hello", email: @resource.email) %></p>

<p><%= t("recrivo.mailer.reset_password.intro") %></p>

<p><%= link_to t("recrivo.mailer.reset_password.action"), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t("recrivo.mailer.reset_password.ignore") %></p>
<p><%= t("recrivo.mailer.reset_password.unchanged") %></p>
