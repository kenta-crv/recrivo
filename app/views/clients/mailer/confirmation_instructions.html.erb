<p><%= t("recrivo.mailer.welcome", email: @email) %></p>

<p><%= t("recrivo.mailer.confirmation.intro") %></p>

<p><%= link_to t("recrivo.mailer.confirmation.action"), confirmation_url(@resource, confirmation_token: @token) %></p>
