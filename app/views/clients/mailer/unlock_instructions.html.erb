<p><%= t("recrivo.mailer.hello", email: @resource.email) %></p>

<p><%= t("recrivo.mailer.unlock.intro") %></p>

<p><%= t("recrivo.mailer.unlock.link_intro") %></p>

<p><%= link_to t("recrivo.mailer.unlock.action"), unlock_url(@resource, unlock_token: @token) %></p>
