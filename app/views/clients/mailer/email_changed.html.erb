<p><%= t("recrivo.mailer.hello", email: @email) %></p>

<% if @resource.try(:unconfirmed_email?) %>
  <p><%= t("recrivo.mailer.email_changed.changing", email: @resource.unconfirmed_email) %></p>
<% else %>
  <p><%= t("recrivo.mailer.email_changed.changed", email: @resource.email) %></p>
<% end %>
